name: CI/CD Pipeline
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Kodni yuklash
        uses: actions/checkout@v4
      - name: Node.js o'rnatish
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Tayyor
        run: echo "HTML CSS JS loyiha muvaffaqiyatli tekshirildi!"
